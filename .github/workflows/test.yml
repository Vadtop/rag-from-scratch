name: Tests

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pytest pytest-asyncio anyio httpx fastapi pydantic pydantic-settings python-dotenv chromadb numpy requests

      - name: Run tests
        env:
          OPENROUTER_API_KEY: test-key
          GOOGLE_SHEETS_ID: ""
        run: |
          python -m pytest tests/ -v --tb=short
